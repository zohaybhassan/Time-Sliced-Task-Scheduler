# Time-Sliced Task Scheduler

A C++ console application that schedules machine-learning tasks by priority and executes them in time slices.

Open `momomomo.sln` in Visual Studio to build the project. A sample task list is available at `momomomo/tasks.txt`.

Each task-list line uses the format `<id> <task type>, <computation time> <priority>`.
